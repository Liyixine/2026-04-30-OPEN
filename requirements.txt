--extra-index-url https://download.pytorch.org/whl/cpu

# Core OpenVINO runtime. Qwen3-VL support in this project follows the official
# ModelScope workshop baseline and needs the newer OpenVINO/Optimum stack.
openvino==2026.0
nncf>=2.15.0

# PyTorch CPU wheels
torch==2.8
torchvision==0.23.0
torchaudio==2.8.0

# Optimum Intel fixed commit used by the official workshop baseline.
git+https://github.com/openvino-dev-samples/optimum-intel.git@2f62e5aee74b4acba3836e1f26678c0db0a09c00

# Model download and multimodal processing
modelscope
# Qwen3-TTS official helper currently pins 4.57.3. Qwen3-ASR is run in a
# separate subprocess that can install/use its own 4.57.6 dependency.
transformers==4.57.3
accelerate
huggingface_hub
qwen-vl-utils
Pillow

# ASR/TTS dependencies
qwen-asr
scipy
soundfile
librosa

# Demo / notebook. Keep <6 to avoid ModelScope ms-swift conflicts in Gallery.
gradio>=3.40,<6.0
jupyterlab
ipywidgets
ipykernel

# Official workshop constrains numpy for compatibility.
numpy<2.0
